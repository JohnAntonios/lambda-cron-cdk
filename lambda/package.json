{
  "name": "src",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/lambda.ts --platform=node --format=cjs --outdir=dist --outbase=src --color=true"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "esbuild": "^0.12.0"
  }
}
